<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <title><%= product.title %></title>
</head>
<body>
    <%- include('nav.ejs', {isLoggedIn:isLoggedIn}) %>
    <br>
    <div id="detail">
        <h1><%= product.title %></h1>
        <img src="<%= product.imgURL %>" alt="<%= product.title %>">
        <h2>Region: <%= product.region %></h2>
        <p><%= product.description %></p>
        <% if (likeList.includes(product.title)) { %> 
            <form action="/like/removeLike/<%= product.title %>" method="GET" class="inline"><button type="submit">Unlike it</button></form>
        <% } else { %>
            <form action="/like/addLike/<%= product.title %>" method="GET" class="inline"><button type="submit">Like it</button></form>
        <% } %>
    </div> 
    <br>
    <h1 id="sameRegion">Team in same region</h1>
    <div id="gallery">
        <% for (let item of products) { %> 
            <% if (product.title !== item.title) { %> 
                <div id="galleryItem">
                    <h3><%= item.title %></h3>
                    <img src="<%= item.imgURL %>" alt="<%= item.title %>">
                    <form action="/product/<%= item.title %>" method="GET" class="inline"><button type="submit" id="description">Description</button></form>
                    
                </div>
            <% } %>
        <% } %>
    </div> 
</body>
</html>